[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/bytebotd/Dockerfile"
buildContext = "packages"

[deploy]
startCommand = "/usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf -n"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

