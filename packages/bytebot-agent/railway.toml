[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/bytebot-agent/Dockerfile"
buildContext = "packages"

[deploy]
startCommand = "npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

